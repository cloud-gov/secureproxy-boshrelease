---
meta:
  environment: NAME
  dns_root: microbosh
  security_groups:
    - secureproxy
  stemcell:
    name: STEMCELL

director_uuid: PLACEHOLDER-DIRECTOR-UUID

releases:
  - name: secureproxy
    version: latest
